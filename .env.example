# Copy to .env and fill in. Vite inlines VITE_-prefixed vars into the client
# bundle at build time — nothing here is a server-side secret.

# Web3Forms access key (web3forms.com) — routes contact-form messages to the
# inbox. Public by design; with no value the form falls back to mailto:.
VITE_WEB3FORMS_ACCESS_KEY=
