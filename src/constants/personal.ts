/**
 * Path into public/ — the file is served as-is. Built from BASE_URL so the
 * link keeps working even if the site ever moves under a subpath again.
 */
export const CV_PDF_PATH = `${import.meta.env.BASE_URL}Roman_Gusevski_CV.pdf`;

export const LOGO_ALT = 'Goose logo';
